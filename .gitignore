# Cursor: project rules and slash commands live under .cursor/ (tracked)

# Specify — local init preferences from specify setup, not source of truth
.specify/init-options.json

# Node / extension build
node_modules/
.npm/
.pnpm-store/
extension/dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Caches (tooling)
.eslintcache
*.tsbuildinfo

# Local env / OS
.env
.env.local
.env.*
!.env.example
.DS_Store

# Chrome Web Store upload artifact (rebuild with `npm run build:zip`)
foc-gh-webstore.zip

# Optional local copy if you regenerate the ID key pair (public stays committed as manifest-id-public.b64)
extension/manifest-id-private.pem
